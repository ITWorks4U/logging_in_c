###
#   October 15th, 2025         version 1.0.0
###
-   offers to create log sessions on stdout or redirection to files
-   offers for file using: NO_ROTATION, DAYLY_ROTATION, SIZE_ROTATION
-   logging on stdout comes with a colorized version
-   comes with test files for all four different log session kinds
-   created readme.md file

###
#   March 25th, 2026           version 1.2.0
###

-   readme.md
    -   added public functions as a simple overview and clean description
    -   updated description

-   logging.h
    -   correction expression from DAYLY_ROTATION to DAILY_ROTATION
    -   added description for LogRotation enumeration
    -   added new function to initialize a log session by using the arguments
    -   handled logic error, when instance to Logging structure points to NULL
    -   handled logic error, when the log name points to NULL
    -   renamed log_message() function to write_to_log()
    -   renamed dispose_logging() function to dispose()

-   logging.c
    -   implementation changes from logging.h
    -   changed macro constant with real length of the used C-Strings in functions: _rotate_log_file_daily(), _rotate_log_files(), write_to_log()
    -   added _internal_log_initializer() function to handle both ways for new log session
    -   check on console moved upwards and leaves the initial function earlier
    -   in case of warnings the message turns into orange on stderr
    -   in case of errors the message turns into red on stderr
    -   unable to log, if one of the initializing functions hasn't been triggered earlier
    -   fixed logical error for keep_nbr_of_files: one more file exists, which shouldn't

-   test files
    -   updated functionalities of the certain test files
    -   offers an alternative way to initialize a log session
    -   renamed file_dayly_rotation.c to file_daily_rotation.c

-   created makefile (for UNIX systems only) / makefile.bat (Windows only)
    -   offers to build the libary automatically
    -   offers to clean up the output file(s)

-   main.c
    -   just a dummy source file without content => only in use to allow to compile the project

###
#   March 27th, 2026           version 1.3.0
###
-   makefile
    -   updated description

-   readme.md
    -   updated to current version and last updated modification

-   logging.h
    -   added version as constant expression
    -   added additional member for LogRotation enumeration

-   logging.c
    -   changed default LogRotation setting from NO_ROTATION to UNSET_ROTATION
    -   level colors comes with a more clean description
    -   moved function _rotate_log_file_daily() behind _rotate_log_files() function
    -   updated function _rotate_log_file_daily()
        -   hasn't used keep_nbr_of_files setting
        -   used an own implementation => added the timestamp behind the last used logging file
    -   updated function _rotate_log_files()
        -   used more meaningful name for C-string "old_name"
        -   function remove() may fail silently => surrounded with access() function to check, if this file exists
    -   added _on_safe_localtime() function
        -   for Windows and UNIX systems this function is for threading, where the real function call comes with a different order for arguments and return value
    -   in case of errors these are going to write into the log file with LOG_ERROR setting
    -   added _generate_last_error_message() function
        -   for Windows only
        -   in case of the error message was unable to create the error number instead is going to use
    -   updated check_for_new_rotation() function
        -   added underline for internal function
        -   macro definition for Windows has been changed from #if _WIN32 to #ifdef _WIN32 to avoid side effects
        -   for UNIX: instead of print a warning message to stderr, the message is going to write into the log file with certain log level
    -   updated _internal_log_initializer function()
        -   numbr of keeping files and file size is set by default
        -   rotation setting will be compared
        -   error messages are still for stderr
        -   C-strings for file handling weren't null terminated and may contain any garbage
        -   special handling for DAILY_ROTATION and SIZE_ROTATION
    -   updated write_to_log function()
        -   check for console only has been moved to top

-   main.c
    -   arguments are able to handle
        -   only for 2 arguments and only for version with "-v" expression
    -   removed duplicated "the" expression in commentary